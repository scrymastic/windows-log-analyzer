


class Reporter:
    def __init__(self):
        pass

    def alert(self, event, rule_id: str):
        # Report the event to the rule_id
        # Get the rule title from /active-rules/headers/{rule_id}.yml
        # Get the rule metadata from /active-rules/metadata/{rule_id}.yml

        pass

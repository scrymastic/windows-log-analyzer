detection:
- selection:
  - and:
    - ScriptBlockText|contains: Test-NetConnection
    - ScriptBlockText|contains: '-ComputerName '
    - ScriptBlockText|contains: '-port '
- filter:
  - and:
    - or:
      - ScriptBlockText|contains: ' 443 '
      - ScriptBlockText|contains: ' 80 '
- condition: selection and not filter
id: adf876b3-f1f8-4aa9-a4e4-a64106feec06
logsource:
  category: ps_script
  definition: 'Requirements: Script Block Logging must be enabled'
  product: windows

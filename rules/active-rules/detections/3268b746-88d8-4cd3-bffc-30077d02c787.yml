detection:
- and:
  - or:
    - CommandLine|contains: ' -NoP -NonI -w Hidden -c $x=$((gp HKCU:Software\Microsoft\Windows
        Update).Update)'
    - CommandLine|contains: ' -NoP -NonI -c $x=$((gp HKCU:Software\Microsoft\Windows
        Update).Update);'
id: 3268b746-88d8-4cd3-bffc-30077d02c787
logsource:
  category: process_creation
  product: windows
